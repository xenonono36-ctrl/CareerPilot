{
  "name": "careerpilot-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root. Vercel must be configured with Root Directory = 'frontend' to deploy the Next.js app. The FastAPI backend in backend/ is deployed separately to Railway."
}
